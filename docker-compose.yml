version: '3.8'

services:
  backend:
    build: ./back
    container_name: slamdunk_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./front
    container_name: slamdunk_frontend
    ports:
      - "8501:8501"  
    restart: always
    depends_on:
      - backend